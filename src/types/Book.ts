export interface Book {
    id: string
    cover: string
    title: string
    author: string
    pages: number
    genres: string[]
    published: string
    language: string
    inLibrary: boolean
}
